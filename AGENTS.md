# AGENTS.md

## 1. 项目定位
本项目为 `PCA_Model_Builder`，用于流程工业历史数据的离线动态 PCA 建模与验证。
当前目标是验证装置状态监控、异常检测和变量贡献分析的工程价值。
项目面向工程师使用，不是企业级工业软件平台。
PCA只用于描述统计偏离，不输出确定性根因、因果关系或控制建议。

## 2. 当前范围
当前阶段只实现离线 DPCA 核心能力。
允许读取历史 CSV、选择建模变量、选择参考状态数据并训练模型。
允许在独立历史窗口中回放验证模型。
允许输出主元得分、T²、SPE/Q、状态等级和变量贡献。
允许保存不包含原始生产数据的安全模型包。
未经任务明确要求，不开发实时服务、数据库、权限系统或企业级后台。

## 3. 明确排除项
不得引入 PLS、监督预测模型或目标变量回归。
不得引入 LLM、RAG、自动报告或自然语言根因解释。
不得把 PCA 贡献变量表述为根因排序。
不得实现自动控制、参数优化、APC替代或闭环控制逻辑。
不得为了“完整平台”主动增加多装置管理、用户管理或云服务。
不得在未获明确任务授权时实现多工况模型。

## 4. 已确认的算法原则
采用动态 PCA，而不是只使用静态 PCA。
所有被选中的连续过程变量统一执行 Lag 扩展。
默认最大 Lag 为60分钟，默认 Lag 步长为5分钟，均必须可配置。
默认使用10分钟尾随滑动平均，参数必须可配置。
平滑必须是因果尾随计算，不得读取未来数据。
平滑和 Lag 不得跨越物理时间缺口。
PCA输入采用基于参考状态数据的 Z-score 标准化。
工程量程不得替代均值和标准差用于 PCA 标准化。
默认按累计解释率95%选择主元，并允许人工指定主元数量。
必须保留有效残差空间，不得选择全部有效秩而使 SPE 失效。

## 5. 状态统计量
必须分别计算并保留 T² 和 SPE/Q。
T²表示保留主元空间中的状态偏离。
SPE/Q表示模型未解释的残差偏离。
T²和SPE分别使用95%和99%控制限。
每个统计量必须有独立的数值、等级和限值比值。
综合状态可以取两者中更严重的等级，但不得替代独立状态。
不得只输出一个综合健康值而丢弃 T²和SPE。
主元得分必须由统一接口输出，至少包含 PC1和PC2。
验证结果推荐保存全部已选主元得分。

## 6. 贡献分析
T²贡献和SPE贡献必须分别计算。
动态特征贡献必须聚合回原始 Tag。
贡献输出可包含主要贡献 Lag 区间。
主要贡献 Lag 区间只能称为贡献区间，不得称为确定过程时滞。
只有某统计量达到95%控制限时，才输出该统计量的异常贡献。
未越界统计量不得被包装成异常贡献。
贡献百分比必须有清晰定义并通过测试验证加总规则。
任何贡献结果都不得写成因果结论或自动根因结论。

## 7. 参考状态与验证
参考状态必须由工程师确认，算法只能提供辅助。
当前至少支持人工选择参考时间窗口。
未来的聚类辅助和性能条件筛选只能在明确任务中增加。
不得自动将某个聚类结果直接定义为正常状态。
训练窗口与验证评分窗口必须相互独立。
验证构造 Lag 特征时可以读取验证起点之前的历史作为上下文。
前置历史只能用于平滑和Lag，不得被计入验证评分结果。
不得因动态预热而无说明地丢失验证起点后的评分。
验证命令不得自动将模型标记为通过。
最终模型有效性必须保留工程师人工确认步骤。

## 8. 数据与Tag配置
输入数据必须包含可解析、唯一且按时间递增的时间戳。
参加建模的Tag必须是有限数值。
缺失值、重复时间戳、非数值、无穷值和采样异常不得静默修复。
发现数据质量问题时应输出明确问题并阻止训练。
时间缺口与普通不规则采样应区分处理，不得简单跨缺口插值。
Tag配置应支持名称、描述、单位、类型、工程量程、正常操作范围和报警范围。
工程量程用于数据有效性检查，不用于 PCA 标准化。
正常操作范围用于工程解释，不直接决定 PCA 控制限。
报警范围仅作为外部工艺信息，不得改变 PCA 数学定义。
自动清洗、插值、重采样或异常值删除必须由任务明确授权。

## 9. 模型包
模型包不得使用不安全的 pickle 反序列化。
优先使用 JSON清单和 NumPy数组等可检查格式。
模型包不得包含原始过程数据。
必须保存特征顺序、均值、标准差、载荷、特征值和解释率。
必须保存主元数量、T²限值、SPE限值和预处理参数。
必须保存训练时间窗口和schema版本。
加载后对相同输入的评分结果必须与保存前完全一致。
模型格式变化必须增加schema兼容测试，不得静默破坏旧模型。
探索模型只能处于 draft，正常状态模型新建时只能处于 candidate；不得通过训练或聚类自动进入 validated 或 published。
schema v1 的 validation_status 只能作为历史来源信息，加载后不得据此升级模型状态。

## 10. 修改边界
只修改当前任务明确涉及的文件和逻辑。
不得顺手重构无关模块。
不得主动增加新算法、重型依赖或大范围架构。
保持现有CLI语义，除非任务明确要求调整。
保持Windows兼容，优先使用 `pathlib` 和跨平台路径处理。
文本文件、CSV和JSON必须明确使用UTF-8或UTF-8-SIG。
不得依赖需要本机编译C/C++扩展的新库。
不得通过删除保护逻辑、放宽校验或吞掉异常来完成任务。

## 11. 测试要求
每个行为修改都必须增加或更新针对性测试。
测试必须覆盖具体文件、具体函数和所有重复实现位置。
测试不能只验证最终输出，还要约束关键旧错误模式消失。
不得通过放宽断言、删除测试或修改测试数据来掩盖问题。
动态预处理测试必须覆盖尾随平滑、Lag顺序和时间缺口。
评分测试必须分别覆盖T²异常、SPE异常和两者同时异常。
贡献测试必须覆盖Tag聚合、百分比加总和越界条件。
模型包测试必须覆盖保存加载往返一致性和非法包拒绝。
验证测试必须覆盖训练/验证隔离和前置历史上下文。
修改CLI后必须增加端到端最小验证测试。
默认运行 `python -m pytest -q`，无法运行时必须说明原因。

## 12. Review 与交付
每次修改完成后，执行 Agent 必须检查实际 patch，不得只依据测试是否通过判断任务完成。
Review 重点检查算法语义、时间边界、数据泄漏、输出误导及修改范围。
普通代码修改任务由执行 Agent 完成自审，不得默认启动独立 Reviewer 子 Agent。
仅当用户或任务明确要求“独立 Review”“双 Agent Review”或指定多 Agent 工作流时，才必须启动与执行 Agent 上下文相互独立的 Reviewer 子 Agent检查实际 patch。
Reviewer 只报告有代码证据的 P0、P1、P2 问题，并标明涉及文件、位置和触发条件。
发现部分修复时，应追加小范围修复，不得混入后续大范围重构。
PR 描述可以简短，但测试和 Review 清单必须完整表达任务边界。交付说明必须列出修改文件、行为变化、测试命令、Review 结果和剩余风险。
未经工程师确认，不得把草稿模型、验证结果或实验算法标记为生产可用。

## 13. 当前项目定位与开发优先级

本项目是本地工业PCA/DPCA建模、历史回放和异常贡献分析工具，不以生产级模型发布平台为目标。

默认不建设或扩展：

- 模型发布系统；
- model registry；
- candidate/validated/published完整生命周期；
- 父子版本谱系；
- 外部SHA sidecar；
- 跨进程模型仓库锁；
- 审计级验证证据链；
- 在线部署和模型发布审批。

后续开发优先级：

1. 正常训练数据选择；
2. PCA与DPCA效果比较；
3. 平滑、Lag和主成分参数评估；
4. T²/SPE阈值与误报警率；
5. 已知异常检出率和检出延迟；
6. 贡献变量排序与事件级解释；
7. 多负荷和多工况适应；
8. 历史回放与使用体验。

除非用户明确提出，不得主动把项目扩展为生产级模型治理或发布平台。
