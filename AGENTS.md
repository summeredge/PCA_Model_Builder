# AGENTS.md

## 项目开发规则

本文件定义代码 Agent 执行规则。
项目设计和技术约束请参考 docs 文档。

---

## 修改原则

- 优先最小范围修改。
- 不进行无关重构。
- 保持已有接口、数据结构和用户行为兼容。
- 不主动增加未要求功能。

---

## 文档优先

涉及以下内容修改时，必须先检查对应文档：

模型包、保存加载：
- docs/model_package_contract.md

预处理、时间序列处理：
- docs/preprocessing_contract.md

评分、状态、贡献计算：
- docs/scoring_contract.md

部署边界：
- docs/deployment_contract.md

不得通过猜测替代已有契约。

---

## 工程约束

修改必须考虑：

- 时间序列边界；
- 数据泄漏风险；
- 模型兼容性；
- Windows运行环境。

禁止：

- 使用pickle保存模型；
- 静默改变字段含义；
- 删除兼容逻辑；
- 扩大项目范围。

---

## 测试要求

代码修改必须：

- 更新相关测试；
- 执行测试命令；
- 验证旧功能未破坏。

默认：

```bash
python -m pytest -q
```

禁止通过删除测试或放宽断言解决问题。

## Review要求

完成修改后检查：

实际patch；
修改范围；
测试结果；
潜在风险。

交付说明包含：

修改文件；
修改内容；
测试结果；
已知风险。

## Git规则

禁止自动：

commit；
push；
创建PR。

完成修改后等待用户确认。
