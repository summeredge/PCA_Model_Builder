# 预处理契约

## 基本原则

离线训练、验证和未来部署必须保持相同预处理语义。

## 固定规则

-   时间戳必须可解析、递增、唯一。
-   物理时间缺口必须识别。
-   平滑和Lag不得跨越物理缺口。
-   滤波必须为因果计算。
-   不自动插值、补点或删除异常值。

## 执行顺序

``` text
时间检查
→ 缺口识别
→ 重采样
→ 数据检查
→ 因果滤波
→ Lag扩展
→ 删除预热不足数据
→ Z-score
→ DPCA评分
```

评分上下文不足或缺口重置期间不得伪造评分结果。
